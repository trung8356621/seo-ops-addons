/**
 * @deprecated Retired production frontend. Canonical React mini-app:
 * resources/js/topical-map/ (isolated Vite build → public/build-topical-map).
 * Keyword Relationship lives in the same bundle as Site Topical Map.
 * Kept only as a non-wired archive reference; not registered in root Vite.
 */
export default function keywordRelationshipChartRetired() {
    console.warn('[KeywordRelationship] keyword-relationship-chart.js is retired. Use the React Topical Map app.');
}
