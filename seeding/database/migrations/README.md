# Seeding migrations (active)

Connection: `omi_seeding`

This directory is the **future** business migration plane.

Current phase:

- DB plane exists and is health-checked
- **No business tables** yet (workspace SoT = browser localStorage)
- Experimental V2 tables that targeted `omi_seo_ai` live under
  `database/legacy-experimental/migrations` and are **not** registered
