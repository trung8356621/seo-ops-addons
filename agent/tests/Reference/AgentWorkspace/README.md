# Agent Workspace tests — REFERENCE ARCHIVE

These PHPUnit files historically proved the **Agent Workspace product** runtime
(chat, packs, planning, seo_agent_* persistence, Agent Workspace automations).

They are **not** part of the active CI suite (`AgentAddon` excludes this tree).

Do not re-enable as runtime requirements. The Agent Workspace DB plane (`seo_agent_*`)
has been retired.
